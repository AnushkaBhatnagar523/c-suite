{
  "name": "c-suite-production",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && node app.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}